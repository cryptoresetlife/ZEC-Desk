# 更新说明

## v0.3.0 — macOS 预览版

- 新增 Apple 芯片 arm64 与 Intel x64 的 macOS .app 打包和原生构建流程，最低 macOS 14。
- 原生 Zingo / Nym 钱包组件，不需要 WSL；独立钱包使用系统文件锁防止并发打开。
- Mac 应用在本机浏览器打开，保留扫描、Noir 连接、自动 mint 与充值地址复制；项目任务通过浏览器打开并手动填写地址。
- 钱包与应用记录保存到用户的 Application Support 目录，替换应用不删除记录。
- 发布包继续仅包含白名单程序文件；不含个人钱包、凭据、运行记录。未进行真实转账或 mint 测试。


## v0.2.2 — 2026-09-21

- 独立钱包新增“复制充值地址”按钮，复制当前选中的完整地址；钱包未连接或地址无效时不允许复制。
- 剪贴板不可用时显示只读地址并自动选中，支持 Ctrl+C 手动复制。
- 补充 u1 统一地址和 t1 透明地址说明，提示转出钱包或交易所须支持目标地址格式；说明 Noir 与 Zingo 独立钱包余额分开。
- 发布包继续排除钱包、助记词、个人地址记录、登录凭据和浏览器资料。

验证：108 项现有测试及脚本语法检查通过；干净包启动和发布文件隐私检查通过。未进行真实转账测试。

升级：先使用“停止并退出软件”，再完整解压新版运行；保留自己的钱包备份及本机数据，勿将其上传。

## v0.2.1 — 2026-09-21

- Grok 搜索改为流式接收进度，将搜索总等待上限从 60 秒延长至 5 分钟；完成前不把片段当作最终项目证据。
- 显示连接、服务处理、搜索和整理阶段及等待秒数；增加“取消本次查询”，保留登录，不自动重试或切换计费通道。
- 区分超时、网络、权限、响应格式和未完成结果；保留最近成功结果。取消或超时不保证服务端停止计费。
- 分开标注 Grok 手动查询与 X API 自动热度扫描；旧后台未加载修复时提示完整退出重启并暂停查询。
- 发布继续采用文件白名单，排除个人记录、钱包、密钥、登录态和浏览器资料。

验证：108 项自动测试通过，覆盖流式分块、超时、取消、异常返回和界面状态；未进行真实付费搜索验收，订阅通道仍为实验兼容。此次更新未改动付款逻辑。

升级：先点击左下角“停止并退出软件”，再更新并重新打开。仅关闭窗口不会停止旧后台；退出后需重新登录 Grok。

## v0.2.0 — 2026-09-21

- 修复独立钱包停在 0%、提示“钱包扫描尚未完成”的问题：升级到 Zingolib v6，支持 NU6.3 / Ironwood 新版交易。
- 补齐 Nym 配套组件、后台同步轮询、错误退避和准确的同步完成判断；保留主网、最新区块和余额预检。
- 修复备份弹窗内容被关闭事件清空的问题；关闭弹窗会清除恢复信息。
- 新增 ZADDR Noir 付款流程：在装有 Noir 的 Chrome 中连接本机 localhost 页面，等待 Public 开放后请求一次付款确认，最终由用户在插件确认。
- 新增项目任务窗口和选定公开钱包地址的辅助填写；项目登录、任务提交和插件签名仍由用户完成。
- 简化导航，将常用功能放到首页，独立钱包和 Zingo 全自动 mint 收到“更多功能”。
- Windows 包使用文件白名单，排除钱包、个人配置、浏览器资料、登录态、任务日志和本机图片缓存。原生组件通过编译路径映射排除构建者用户名。

验证：100 项自动测试通过；新版钱包完成主网只读同步验证。没有进行真实付款或 mint 测试。自动付款仍仅适配 ZADDR Public。

升级：退出旧版后完整解压新版。原有钱包位于 WSL Ubuntu 用户目录，与程序分开；升级前请先备份。保留原有 `data/` 可保留本机配置和防重复付款记录，但不要把它分享或上传。Noir 需要 Chrome 和插件确认，Zingo 需要 WSL Ubuntu；两者的钱包余额独立。

## v0.1.0 — 2026-09-21

- 首次公开发布 ZEC Desk 源码和 Windows x64 便携程序包。
- ZADDR Public 监控与预算确认后的自动 mint，独立 WSL Zcash 钱包。
- zebra.family 市场监控、远程项目图片、自选官网、目标价提醒。
- 早期资格与 X 热度线索，Grok 官方设备授权登录和手动 X 搜索研究。
- 补齐 Grok 订阅接口版本兼容请求头；凭据隔离、取消和失败处理回归测试。
- 发布采用文件白名单；排除个人配置、日志、钱包、浏览器数据和 NFT 图片缓存。

本版本为预览版。自动 mint 只适配 ZADDR Public，其他项目只做发现与观察。
