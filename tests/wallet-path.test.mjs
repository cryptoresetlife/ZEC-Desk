import test from 'node:test';import assert from 'node:assert/strict';import {walletBinaryPath} from '../lib/wallet.mjs';
test('WSL path conversion bypasses shell parsing and uses forward slashes',async()=>{let call;const p=await walletBinaryPath('C:\\Users\\Example Name\\ZEC Desk',async(...args)=>{call=args;return {stdout:'/mnt/c/Users/Example Name/ZEC Desk/native/zingo-deskwallet\n'};});assert.equal(call[0],'wsl.exe');assert.deepEqual(call[1].slice(0,5),['-d','Ubuntu','--exec','wslpath','-u']);assert(!call[1][5].includes('\\'));assert.equal(p,'/mnt/c/Users/Example Name/ZEC Desk/native/zingo-deskwallet');});
test('path errors and unexpected output do not leak shell output or local profile paths',async()=>{for(const run of [async()=>{throw Error('private command and stderr');},async()=>({stdout:'bad\nsecond line'})])await assert.rejects(walletBinaryPath('C:/example',run),e=>e.message.includes('WSL Ubuntu')&&!e.message.includes('private command')&&!e.message.includes('C:/example'));});
