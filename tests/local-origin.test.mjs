import test from 'node:test';import assert from 'node:assert/strict';import {localOrigin,localRequestAllowed} from '../lib/local-origin.mjs';
test('Noir localhost and desktop IPv4 are the only accepted hosts',()=>{assert.equal(localOrigin('localhost:8793'),'http://localhost:8793');assert.equal(localOrigin('127.0.0.1:8793'),'http://127.0.0.1:8793');for(const x of [undefined,'localhost.evil.example:8793','evil.example:8793','localhost:80','localhost.:8793','0.0.0.0:8793','127.0.0.1:8793@evil.example','[::1]:8793'])assert.equal(localOrigin(x),null);});
test('local API still requires its session token and matching exact origin',()=>{const token='test-session';for(const host of ['localhost:8793','127.0.0.1:8793']){assert(localRequestAllowed({host,origin:'http://'+host,'x-zec-desk':token},token));assert(!localRequestAllowed({host,origin:'http://'+host,'x-zec-desk':'wrong'},token));assert(!localRequestAllowed({host,origin:'null','x-zec-desk':token},token));assert(!localRequestAllowed({host,origin:'https://evil.example','x-zec-desk':token},token));}assert(!localRequestAllowed({host:'localhost:8793',origin:'http://127.0.0.1:8793','x-zec-desk':token},token));});
