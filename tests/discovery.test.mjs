import test from 'node:test';import assert from 'node:assert/strict';import {candidates,publicUrl,earlySignal} from '../lib/discovery.mjs';
test('discovery rejects scripts and deduplicates project links',()=>{const r=candidates('<title>Index</title><a href="javascript:bad()">mint</a><a href="/mint">Mint</a><a href="/mint">Mint 2</a>','https://example.org/');assert.equal(r.length,2);assert.equal(r[1].url,'https://example.org/mint');});
test('discovery does not treat private addresses or URL credentials as public sources',async()=>{for(const url of ['http://example.org','https://127.0.0.1','https://localhost','https://user:pass@example.org','https://[::1]'])await assert.rejects(publicUrl(url));});
test('early application links are discovered even without mint or NFT keywords',()=>{const out=candidates('<title>Zcash Art</title><a href="/whitelist">Join</a><a href="/early-access">Early Access</a><a href="/apply">Apply</a><a href="/waitlist">Waitlist closed</a>','https://example.org/');assert.equal(out.length,5);for(const x of out.slice(1))assert.ok(x.earlySignals.length);assert.match(out.at(-1).kind,/未核实/);});
test('hidden scripts and unrelated application links are not early project evidence',()=>{assert.deepEqual(earlySignal('<script>whitelist()</script><style>.allowlist{}</style>'),[]);assert.equal(candidates('<title>Careers</title><a href="/apply">Apply</a>','https://example.org/').length,1);});
